apply plugin: 'java'
/*For some reason, gradle build fails without this next bit - or if it's 1.8*/
sourceCompatibility = 1.7
targetCompatibility = 1.7

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}